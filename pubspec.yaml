name: mysql_cli_client
version: 1.0.0
description: A simple wrapper for calling the mysql command line client from Dart.
repository: https://github.com/onepub-dev/mysql_cli_client
environment: 
  sdk: ^3.0.2
dependencies: 
  dcli: ^3.0.2
  lint_hard: ^4.0.0
dev_dependencies: 
  lints: ^2.0.0
  test: ^1.21.0
